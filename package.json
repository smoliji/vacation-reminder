{
  "name": "vacation-reminder",
  "scripts": {
    "test": "node --require ts-node/register --test src/**/*.test.ts",
    "build": "npx esbuild src/index.ts --bundle --format=iife --global-name=app --target=es2020 --outfile=dist/app.js"
  },
  "author": "Jiri Smolik",
  "license": "MIT",
  "devDependencies": {
    "@types/google-apps-script": "^1.0.83",
    "esbuild": "^0.21.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
